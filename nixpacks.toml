[phases.setup]
nixPkgs = ["nodePackages.npm", "nodejs"]

[phases.build]
cmds = ["cd backend && npm install && npm run build && cd ../frontend && npm install && npm run build && mkdir -p ../backend/dist/public && cp -rv dist/* ../backend/dist/public/"]

[start]
cmd = "cd backend && npm start"
