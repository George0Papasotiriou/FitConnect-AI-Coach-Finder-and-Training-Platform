{
  "name": "fitconnect-monorepo",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "build": "cd backend && npm install && npm run build && cd ../frontend && npm install && npm run build && mkdir -p ../backend/dist/public && cp -rv dist/* ../backend/dist/public/",
    "start": "cd backend && npm start"
  }
}
